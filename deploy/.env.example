# Copy to deploy/.env and fill values.

# RuTracker (requires a valid authenticated cookie string).
SEARCH_PROVIDER_RUTRACKER_COOKIE=
SEARCH_PROVIDER_RUTRACKER_PROXY=

# Optional default FlareSolverr URL shown in Settings (Docker default is used if empty).
# FLARESOLVERR_URL=http://flaresolverr:8191/

# Traefik observability
# /metrics is exposed on Traefik dashboard port (default: http://localhost:8081/metrics)
# Prometheus UI: http://localhost:9090
# Jaeger UI: http://localhost:16686
# Grafana UI: http://localhost:3000 (default admin/admin)
# GRAFANA_ADMIN_USER=admin
# GRAFANA_ADMIN_PASSWORD=admin
# TRAEFIK_TRACE_SAMPLE_RATE=1.0
# TRAEFIK_OTLP_HTTP_ENDPOINT=http://jaeger:4318/v1/traces


