# ============================================================
# REQUIRED — set these before running docker compose up
# ============================================================

# Grafana admin password (no default — must be set explicitly)
GRAFANA_ADMIN_PASSWORD=REPLACE_ME

# Copy to deploy/.env and fill values.

# RuTracker (requires a valid authenticated cookie string).
SEARCH_PROVIDER_RUTRACKER_COOKIE=
SEARCH_PROVIDER_RUTRACKER_PROXY=

# Optional default FlareSolverr URL shown in Settings (Docker default is used if empty).
# FLARESOLVERR_URL=http://flaresolverr:8191/

# FlareSolverr proxy configuration (leave empty to disable proxy)
FLARESOLVERR_PROXY_URL=

# Traefik observability
# /metrics is exposed on Traefik dashboard port (default: http://localhost:8081/metrics)
# Prometheus UI: http://localhost:9090 (localhost only)
# Jaeger UI:     http://localhost:16686 (localhost only)
# Grafana UI:    http://localhost:3000  (localhost only, login with GRAFANA_ADMIN_PASSWORD above)
# GRAFANA_ADMIN_USER=admin
# TRAEFIK_TRACE_SAMPLE_RATE=1.0
# TRAEFIK_OTLP_HTTP_ENDPOINT=http://jaeger:4318/v1/traces

# ============================================================
# BACKUP (optional but recommended)
# ============================================================
# Enable scheduled daily MongoDB backup (runs at 3 AM).
# To start: docker compose --profile backup-scheduled up -d
# To run a one-off backup: docker compose --profile backup run --rm mongo-backup
# Backups are stored in the mongo_backups Docker volume.


